<Alloy>
	<Collection src="incident"/>
    <Window class="container">
    	<View>
    	</View>
        <Widget id="incidentsWidget" src="nl.fokkezb.pullToRefresh" onRelease="fnIncidentsRefresh">
            <ListView>
				<HeaderView>
			    	<Label backgroundColor="#a88"> A | B</Label>
				</HeaderView>
                <ListSection dataCollection="incident">
                    <ListItem title="{subject}" />
                </ListSection>
            </ListView>
        </Widget>
    </Window>
</Alloy>
